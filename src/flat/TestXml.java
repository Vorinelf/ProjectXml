package flat;

public class TestXml {
    public static void main(String[] args) {
        Encode encode = new Encode();
        encode.getEncodeToXml();
        Parser parser = new Parser();
        parser.getParser();
    }
}
